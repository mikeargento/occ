// Empty stub package — placeholder for legacy mock imports.
// The mock-enclave (in commit-service) historically depended on @bitgraph/stub
// for a StubHost host implementation. The host has since been deleted; this
// package exists only so `npm install` doesn't fail in commit-service.
export {};
